export enum MediaType {
  video = 'video',
  audio = 'audio',
  skeleton = 'skeleton',
  spline = 'spline',
  data = 'data',
  record = 'record',
  image = 'image',
  dancer = 'dancer',
}

export enum Instructions {
  start = 'start',
  stop = 'stop',
}

export enum EvaluationType {
  A = 'A',
  B1 = 'B1',
  B2 = 'B2',
}

export enum ModuleType {
  A = 'A',
  B = 'B',
}

export enum Phase {
  A,
  B,
  C,
}

export enum Execption {
  NoSuchKey = 'NoSuchKey',
}
